# Copy to .env to override docker-compose defaults.
DB_USER=postgres
DB_PASS=postgres
DB_NAME=whiteboard

JWT_SECRET=super-secret-change-in-production
JWT_EXPIRES_IN=7d

# Backend base URL the browser calls (baked into the frontend build).
NEXT_PUBLIC_API_URL=http://localhost:3000
